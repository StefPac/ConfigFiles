;,--------------------------------------
;| MAIN GROUP: macbook
;`--------------------------------------
;; Settings useful on a macbook pro

;; you can set a value to one variable


(global-set-key (kbd "<f9>") (kbd "<insert>"))
(setq visible-bell nil)

(provide 'spacific-macbook)
